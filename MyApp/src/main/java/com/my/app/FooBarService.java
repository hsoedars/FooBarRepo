package com.my.app;

/**
 * This service converts numbers into FooBar values.
 *
 * It outputs output "Foo" for multiples of three instead of the number and for the multiples of five outputs "Bar".
 * For numbers which are multiples of both three and five print "FooBar".
 * Otherwise the number is returned.
 *
 * This interface should have these methods:
 *
 * 1. takes a number and returns its FooBar value
 *
 * 2. takes a number range (start, end) and returns a List of FooBar values for the range
 *
 * 3. sets a number to an arbitrary value
 *
 * 4. reset a value to its original value
 */
interface FooBarService {

  // TODO
//  String getNumberValue(int i);
//
//  List<String> getNumberValues(int start, int end);
//
//  String setNumberValue(int i, String value);
//
//  String resetNumberValue(int i);

}
